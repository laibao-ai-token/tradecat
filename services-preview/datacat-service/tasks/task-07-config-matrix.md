# 任务 07：配置矩阵与默认值对齐（细化版）

## 目标

- 建立原变量与 DATACAT_* 变量的精确映射。

## 子任务拆分

- 07-01：配置矩阵表最终确认
- 07-02：默认值与优先级规则落地
- 07-03：文档同步

## 配置矩阵（核心）

- DATABASE_URL → DATACAT_DATABASE_URL
- HTTP_PROXY/HTTPS_PROXY → DATACAT_HTTP_PROXY/DATACAT_HTTPS_PROXY
- DATA_SERVICE_LOG_DIR → DATACAT_LOG_DIR
- DATA_SERVICE_DATA_DIR → DATACAT_DATA_DIR
- BINANCE_WS_GAP_INTERVAL → DATACAT_WS_GAP_INTERVAL
- BINANCE_WS_GAP_LOOKBACK → DATACAT_WS_GAP_LOOKBACK
- BINANCE_WS_SOURCE → DATACAT_WS_SOURCE
- KLINE_DB_SCHEMA → DATACAT_DB_SCHEMA
- BINANCE_WS_DB_EXCHANGE → DATACAT_DB_EXCHANGE
- BINANCE_WS_CCXT_EXCHANGE → DATACAT_CCXT_EXCHANGE
- RATE_LIMIT_PER_MINUTE → DATACAT_RATE_LIMIT_PER_MINUTE
- MAX_CONCURRENT → DATACAT_MAX_CONCURRENT
- BACKFILL_MODE → DATACAT_BACKFILL_MODE
- BACKFILL_DAYS → DATACAT_BACKFILL_DAYS
- BACKFILL_START_DATE → DATACAT_BACKFILL_START_DATE
- BACKFILL_ON_START → DATACAT_BACKFILL_ON_START

## 验收

- 所有原变量可复用。
- DATACAT_* 有效覆盖。


## 进度

- 已完成：07-01 配置矩阵确认
- 已完成：07-02 优先级规则落地
- 已完成：07-03 文档同步


## 完成状态

- 07-01/07-02/07-03 已完成。
