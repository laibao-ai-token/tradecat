# 任务 09：依赖与路径隔离（细化版）

## 目标

- 依赖对齐原服务。
- 日志/缓存/ban 状态文件隔离。

## 子任务拆分

- 09-01：依赖清单更新
- 09-02：路径隔离策略实施
- 09-03：限流状态文件隔离

## 执行步骤（更细）

1) 更新 requirements 与 pyproject 依赖。
2) 绑定 DATACAT_LOG_DIR / DATACAT_DATA_DIR。
3) 修改限流状态文件路径为新目录。

## 验收

- 新旧服务互不污染。
- 依赖安装后可运行。
