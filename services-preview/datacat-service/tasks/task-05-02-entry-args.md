# 任务 05-02：入口参数与子进程指向

## 目标

- 入口参数与原服务一致。

## 执行记录（已完成）

- 已在 `src/__main__.py` 复制 Scheduler，并保留 `--ws/--metrics/--backfill/--all`。
- 子进程路径指向严格分层 collector.py。

## 验收

- --ws/--metrics/--backfill/--all 可用。
