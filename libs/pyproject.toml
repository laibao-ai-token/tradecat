[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tradecat-common"
version = "0.1.0"
description = "Shared common utilities for TradeCat services"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["common", "common.utils"]
